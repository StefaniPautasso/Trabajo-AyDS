<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= @section.title %></title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet"> <!-- Font Awesome for icons -->
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Lora:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Lora', serif;
      background: url('/images/background.png') no-repeat center center fixed;
      background-size: cover;
      height: 100vh;
      color: #ffffff; /* Letra blanca */
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }
    .container {
      width: 80%; /* Ancho del contenedor principal */
      max-width: 800px; /* Máximo ancho del contenedor principal */
      padding: 20px;
      background: rgba(255, 255, 255, 0.7); /* Fondo blanco semi-transparente */
      border-radius: 10px;
      box-shadow: 0px 0px 15px 0px rgba(0,0,0,0.1); /* Sombra suave */
      text-align: center; /* Alineación del contenido centrado */
      margin-top: 20px; /* Margen superior */
      height: 60%; /* Altura del contenedor reducida */
      overflow-y: auto; /* Scroll vertical si es necesario */
      display: flex;
      flex-direction: column;
      justify-content: flex-start; /* Alineación del contenido al inicio */
      gap: 20px; /* Espacio entre elementos internos */
    }
    h1 {
      font-family: 'Dancing Script', cursive;
      font-size: 3.5rem; /* Tamaño de la fuente aumentado */
      margin-bottom: 20px;
      text-transform: capitalize; /* Texto con la primera letra en mayúscula */
      color: #dc3545; /* Rojo */
    }
    ul {
      list-style-type: none; /* Quitamos los puntos de la lista */
      padding: 0;
      margin: 0;
      text-align: center; /* Alineación del texto centrado */
    }
    ul li {
      margin-bottom: 15px; /* Separación aumentada entre elementos de la lista */
    }
    ul li a {
      display: block;
      padding: 12px 20px; /* Aumento del padding para hacer los recuadros más grandes */
      background-color: #dc3545; /* Fondo rojo */
      border: 2px solid #dc3545; /* Borde rojo */
      color: #ffffff; /* Letra blanca */
      text-decoration: none;
      font-size: 1.2rem; /* Tamaño de la fuente reducido */
      transition: all 0.3s ease;
      border-radius: 5px;
    }
    ul li a:hover {
      background-color: #ffffff; /* Fondo blanco al hacer hover */
      color: #dc3545; /* Letra roja al hacer hover */
      text-decoration: none; /* Quitamos el subrayado al hacer hover */
    }
    .back-link {
      position: fixed;
      top: 20px;
      left: 20px;
      text-decoration: none;
      color: #dc3545; /* Rojo */
      transition: color 0.3s ease;
      font-size: 1.2rem; /* Tamaño más grande */
    }
    .back-link:hover {
      text-decoration: underline; /* Subrayado al hacer hover */
      color: #ffffff; /* Letra blanca al hacer hover */
    }
    .logout-link {
      position: fixed;
      top: 20px;
      right: 20px;
      text-decoration: none;
      color: #dc3545; /* Rojo */
      transition: color 0.3s ease;
      font-size: 1.2rem; /* Tamaño más grande */
    }
    .logout-link:hover {
      text-decoration: underline; /* Subrayado al hacer hover */
      color: #ffffff; /* Letra blanca al hacer hover */
    }
    .lesson-list {
      width: 70%; /* Ancho reducido para la lista de lecciones */
      margin: 0 auto; /* Centrado horizontal */
      padding: 0; /* Quitamos el padding para ajustar mejor */
    }
    .lesson-item {
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <a href="/menu" class="back-link"><i class="fas fa-arrow-left"></i> Volver al menú</a>
  <div class="container">
    <h1><%= @section.title %></h1>
    <ul class="lesson-list">
      <% @section.lessons.each do |lesson| %>
        <li class="lesson-item"><a href="/sections/<%= @section.id %>/lessons/<%= lesson.id %>"><%= lesson.title %></a></li>
      <% end %>
    </ul>
  </div>
  <a href="/logout" class="logout-link">Cerrar sesión</a>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>

