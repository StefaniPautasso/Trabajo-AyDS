<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Consultas de Preguntas</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Lora:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Lora', serif;
      background: url('/images/background.png') no-repeat center center fixed;
      background-size: cover;
      height: 100vh;
      color: #000000;
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      position: relative;
    }
    .container {
      width: 95%;
      max-width: 900px;
      max-height: 85vh;
      overflow-y: auto;
      padding: 30px;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 10px;
      box-shadow: 0px 0px 20px 0px rgba(0,0,0,0.2);
      margin-top: 20px;
      margin-bottom: 20px;
    }
    h1 {
      font-family: 'Dancing Script', cursive;
      color: #dc3545;
      font-size: 3.5rem;
      margin-bottom: 20px;
    }
    ul {
      list-style-type: none;
      padding: 0;
      margin-top: 20px;
    }
    ul li {
      margin-bottom: 10px;
    }
    ul li a {
      display: block;
      padding: 15px 20px;
      background-color: transparent;
      border: 2px solid #dc3545;
      color: #dc3545;
      text-decoration: none;
      font-size: 1.5rem;
      transition: all 0.3s ease;
      border-radius: 5px;
      width: 100%;
      box-sizing: border-box;
    }
    ul li a:hover {
      background-color: #dc3545;
      color: #ffffff;
      text-decoration: none;
    }
    .btn-custom {
      margin: 5px;
      color: white;
      font-size: 1.25rem;
      font-weight: bold;
      transition: background-color 0.3s ease, color 0.3s ease;
      position: absolute;
    }
    .btn-back {
      background-color: #dc3545;
      left: 20px;
      top: 20px;
    }
    .btn-back:hover {
      background-color: #f76c6c;
    }
    .table-container {
      display: none;
      margin-top: 20px;
    }
    .table th, .table td {
      font-size: 1.2rem;
    }
  </style>
</head>
<body>
  <a href="/menu_admin" class="btn btn-custom btn-back">
    <i class="fas fa-arrow-left"></i> Volver al Menú
  </a>

  <div class="container">
    <h1>Consultas de Preguntas</h1>

    <ul>
      <li>
        <a href="javascript:void(0)" onclick="toggleTable('incorrectTable')">Las 5 preguntas más contestadas de forma incorrecta</a>
      </li>
      <li>
        <a href="javascript:void(0)" onclick="toggleTable('correctTable')">Las 5 preguntas más contestadas de forma correcta</a>
      </li>
    </ul>

    <div id="incorrectTable" class="table-container">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Pregunta</th>
            <th>Total Respuestas Incorrectas</th>
          </tr>
        </thead>
        <tbody>
          <% @incorrect_answers.each do |question_content, total_incorrect| %>
            <tr>
              <td><%= question_content %></td>
              <td><%= total_incorrect %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div id="correctTable" class="table-container">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Pregunta</th>
            <th>Total Respuestas Correctas</th>
          </tr>
        </thead>
        <tbody>
          <% @correct_answers.each do |question_content, total_correct| %>
            <tr>
              <td><%= question_content %></td>
              <td><%= total_correct %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

  </div>

  <script>
    function toggleTable(tableId) {
      const table = document.getElementById(tableId);
      if (table.style.display === 'none' || table.style.display === '') {
        table.style.display = 'block';
      } else {
        table.style.display = 'none';
      }
    }
  </script>
</body>
</html>

