<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registrarse</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Lora:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Lora', serif;
      background: url('/images/background.png') no-repeat center center fixed;
      background-size: cover;
      height: 100vh;
      color: #dc3545; /* Rojo */
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
    }
    .container {
      max-width: 600px;
      padding: 50px;
      background: rgba(255, 255, 255, 0.7); /* Fondo blanco semi-transparente */
      border-radius: 10px;
    }
    h1 {
      font-family: 'Dancing Script', cursive;
      font-size: 4rem; /* Tamaño de la fuente aumentado */
      margin-bottom: 20px;
      text-transform: capitalize; /* Texto con la primera letra en mayúscula */
    }
    .register-form {
      text-align: left; /* Formulario alineado a la izquierda */
      margin-bottom: 20px;
    }
    .register-form label {
      font-weight: bold;
      display: block; /* Para que las etiquetas ocupen una línea completa */
      margin-bottom: 5px; /* Espacio entre etiquetas */
    }
    .register-form input[type="text"],
    .register-form input[type="password"] {
      width: calc(100% - 20px); /* Ancho del campo menos el padding */
      padding: 10px;
      margin-bottom: 20px;
      border: 1px solid #ced4da; /* Gris claro */
      border-radius: 5px;
      box-sizing: border-box;
      display: inline-block; /* Mostrar en línea */
      vertical-align: middle; /* Alinear verticalmente */
    }
    .register-form button {
      background-color: #dc3545; /* Rojo */
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      font-size: 1.25rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
      display: block; /* Botón ocupa una línea completa */
      margin: 0 auto; /* Centrar botón */
    }
    .register-form button:hover {
      background-color: #f76c6c; /* Rojo más claro */
    }
    .register-link {
      font-size: 1.1rem;
      margin-top: 20px; /* Espacio arriba del enlace de registro */
      text-align: center; /* Alinear al centro */
    }
    .register-link p {
      display: inline-block; /* Para que el texto y el enlace estén en línea */
      margin-right: 10px; /* Espacio entre la pregunta y el enlace */
    }
    .register-link a {
      color: #dc3545; /* Rojo de tono medio */
      text-decoration: none;
      font-weight: bold;
    }
    .register-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Registrarse</h1>
    <form action="/register" method="POST" class="register-form">
      <div style="margin-bottom: 20px;">
        <label for="name">Nombre de usuario:</label>
        <input type="text" id="name" name="name" required>
      </div>
      
      <div style="margin-bottom: 20px;">
        <label for="password">Contraseña:</label>
        <input type="password" id="password" name="password" required>
      </div>
        
			<% if error_messages.any? %>
				<p style="color: red;"><%= @error %></p>
				<ul>
				  <% error_messages.each do |error| %>
				    <li><%= error %></li>
				  <% end %>
				</ul>
			<% end %>
	</div>
      
      <button type="submit">Registrarse</button>
    </form>
    
    <div class="register-link">
      <p>¿Ya tienes una cuenta?</p>
      <a href="/login">Iniciar sesión</a>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>



