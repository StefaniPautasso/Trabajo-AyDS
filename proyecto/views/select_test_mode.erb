<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Selecciona el modo del Test</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet"> 
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Open+Sans:wght@400;700&display=swap" rel="stylesheet"> 
  <style>
    body {
      font-family: 'Open Sans', sans-serif; 
      background: url('/images/background2.png') no-repeat center center fixed;
      background-size: cover;
      height: 100vh;
      color: #000000;
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      position: relative;
    }
    .container {
      width: 95%;
      max-width: 900px;
      max-height: 85vh;
      overflow-y: auto;
      padding: 30px;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 10px;
      box-shadow: 0px 0px 20px 0px rgba(0,0,0,0.2);
      margin-top: 20px;
      margin-bottom: 20px;
      text-align: center; 
    }
    h1 {
      font-family: 'Dancing Script', cursive;
      font-size: 3rem; 
      margin-bottom: 20px;
      text-transform: capitalize;
      color: #dc3545;
    }
    p {
      font-size: 1.25rem; 
      line-height: 1.6; 
    }
    .btn-custom {
      margin: 5px;
      color: white;
      font-size: 1.25rem;
      font-weight: bold;
      transition: background-color 0.3s ease, color 0.3s ease;
      position: absolute;
    }
    .btn-back {
      background-color: #dc3545;
      left: 20px;
      top: 20px;
    }
    .btn-logout {
      background-color: #dc3545;
      right: 20px;
      top: 20px;
    }
    .btn-section {
      display: inline-block;
      padding: 12px 18px;
      margin: 10px;
      background-color: #dc3545;
      border: 2px solid #dc3545;
      color: #ffffff;
      text-decoration: none;
      font-size: 1.2rem;
      transition: background-color 0.3s ease;
      border-radius: 5px;
    }
    .btn-section:hover {
      background-color: #f76c6c;
      color: #ffffff;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <a href="/menu" class="btn btn-custom btn-back">
    <i class="fas fa-arrow-left"></i> Volver al menú
  </a>
  <div class="container">
    <h1>Selecciona el modo del Test</h1>
    <p>¿Cómo deseas realizar este test?</p>
    <form action="/sections/<%= @section.id %>/select_test_mode" method="post" style="display: inline-block;">
      <input type="hidden" name="test_mode" value="timed" />
      <button type="submit" class="btn btn-section">
        Modo Cronometrado
      </button>
    </form>
    <form action="/sections/<%= @section.id %>/select_test_mode" method="post" style="display: inline-block; margin-left: 10px;">
      <input type="hidden" name="test_mode" value="normal" />
      <button type="submit" class="btn btn-section">
        Modo Normal
      </button>
    </form>
  </div>
  
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>