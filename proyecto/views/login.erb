<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Iniciar Sesión</title>
  <link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body id="login">
  <div class="container">
    <!-- Agregamos solo la imagen derecha -->
    <img src="/images/imgright.png" class="right-image" alt="Imagen Derecha">
    <h1>BIENVENIDO</h1>
    <% if params[:registration_success] == "true" %>
      <div class="alert alert-success" role="alert">
        Usuario creado con éxito. Por favor, inicie sesión.
      </div>
    <% end %>
    <form action="/login" method="post" class="login-form">
      <label for="name">Usuario:</label>
      <input type="text" id="username" name="name" required>
      
      <label for="password">Contraseña:</label>
      <input type="password" id="password" name="password" required>
      
      <% if @error %>
        <p style="color: red;"><%= @error %></p>
      <% end %>
      
      <!-- Cambiamos el botón de inicio de sesión por un enlace -->
      <button type="submit">Iniciar Sesión</button>
    </form>
    
    <div class="register-link">
      <p>¿No tienes una cuenta? <a href="/register">Registrarse</a></p>
    </div>
  </div>
</body>
</html>




