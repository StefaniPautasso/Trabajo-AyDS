<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Secciones</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet"> <!-- Font Awesome for icons -->
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Lora:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Lora', serif;
      background: url('/images/background.png') no-repeat center center fixed;
      background-size: cover;
      height: 100vh;
      color: #ffffff; /* Letra blanca */
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }
    .container {
      width: 80%; /* Ancho del contenedor principal */
      max-width: 800px; /* Máximo ancho del contenedor principal */
      padding: 20px;
      background: rgba(255, 255, 255, 0.7); /* Fondo blanco semi-transparente */
      border-radius: 10px;
      box-shadow: 0px 0px 15px 0px rgba(0,0,0,0.1); /* Sombra suave */
      text-align: center; /* Alineación del contenido centrado */
      margin-top: 20px; /* Espacio superior ajustado */
      margin-bottom: 20px; /* Espacio inferior ajustado */
    }
    h1 {
      font-family: 'Dancing Script', cursive;
      font-size: 2.5rem; /* Tamaño de la fuente */
      margin-bottom: 20px;
      text-transform: capitalize; /* Texto con la primera letra en mayúscula */
      color: #dc3545; /* Rojo */
    }
    ul {
      list-style-type: none; /* Quitamos los puntos de la lista */
      padding: 0;
      margin-top: 20px;
      text-align: center; /* Alineación del texto centrado */
    }
    ul li {
      margin-bottom: 10px;
    }
    ul li a {
      display: block;
      padding: 12px 18px; /* Ajuste del padding */
      margin: 10px auto; /* Margen horizontal automático y vertical 10px */
      background-color: #dc3545; /* Fondo rojo */
      border: 2px solid #dc3545; /* Borde rojo */
      color: #ffffff; /* Letra blanca */
      text-decoration: none;
      font-size: 1.2rem; /* Tamaño de la fuente */
      transition: background-color 0.3s ease; /* Transición suave del color de fondo */
      width: 70%; /* Ancho del recuadro de sección */
      max-width: 400px; /* Máximo ancho del recuadro de sección */
      border-radius: 5px;
    }
    ul li a:hover {
      background-color: #ffffff; /* Fondo blanco al hacer hover */
      color: #dc3545; /* Letra roja al hacer hover */
      text-decoration: none; /* Quitamos el subrayado al hacer hover */
    }
    .back-link {
      position: absolute; /* Posición absoluta */
      top: 20px;
      left: 20px;
      text-decoration: none;
      color: #dc3545; /* Rojo */
      transition: color 0.3s ease;
      font-size: 1.5rem; /* Tamaño de fuente más grande */
    }
    .back-link:hover {
      text-decoration: underline; /* Subrayado al hacer hover */
      color: #dc3545; /* Letra roja al hacer hover */
    }
    .logout-link {
      position: absolute; /* Posición absoluta */
      top: 20px;
      right: 20px;
      text-decoration: none;
      color: #dc3545; /* Rojo */
      transition: color 0.3s ease;
      font-size: 1.5rem; /* Tamaño de fuente más grande */
    }
    .logout-link:hover {
      text-decoration: underline; /* Subrayado al hacer hover */
      color: #dc3545; /* Letra roja al hacer hover */
    }
  </style>
</head>
<body>
  <a href="/menu" class="back-link"><i class="fas fa-arrow-left"></i> Volver al menú</a>
  <div class="container">
    <h1>Secciones Disponibles</h1>
    <ul>
      <% @sections.each do |section| %>
        <li><a href="/sections/<%= section.id %>"><%= section.title %></a></li>
      <% end %>
    </ul>
  </div>
  <a href="/logout" class="logout-link">Cerrar sesión</a>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>

